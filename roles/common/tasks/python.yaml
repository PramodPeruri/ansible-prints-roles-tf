- name: install python package
  ansible.builtin.dnf:
    name: "{{ item }}"
    state: present
  loop:
  - python3
  - gcc
  - python3-devel
  - python3-pip

- name: install dependencies
  ansible.builtin.pip:
    requirements: /app/requirements.txt
    executable: pip3
  args:
    chdir: /app